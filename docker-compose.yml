version: '3.9'

services:
  messaging-service:
    build: .
    ports:
      - '4000: 3000'
      - '8800: 8800'
    networks:
      - webnet

networks:
  default:
    external: true
    name: gono-jobs-messaging-service
